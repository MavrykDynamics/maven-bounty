{ parameter
    (or (or (or (or (or (nat %acceptOffer) (unit %claimSuperAdmin))
                    (or (pair %createListing
                           (address %initiator)
                           (or %token
                              (address %fa12Token)
                              (pair %fa2Token (address %tokenContractAddress) (nat %tokenId)))
                           (nat %price)
                           (nat %amount)
                           (option %expiryTime timestamp)
                           (or %currency
                              (or (address %fa12) (pair %fa2 (address %tokenContractAddress) (nat %tokenId)))
                              (unit %tez)))
                        (list %mistakenTransfer
                           (pair (address %to_)
                                 (nat %amount)
                                 (or %token
                                    (or (address %fa12) (pair %fa2 (address %tokenContractAddress) (nat %tokenId)))
                                    (unit %tez))))))
                (or (or (pair %offer
                           (nat %listingId)
                           (nat %price)
                           (option %expiryTime timestamp)
                           (or %currency
                              (or (address %fa12) (pair %fa2 (address %tokenContractAddress) (nat %tokenId)))
                              (unit %tez)))
                        (unit %pauseAll))
                    (or (nat %purchase) (address %removeAdmin))))
            (or (or (or (nat %removeListing) (nat %removeOffer))
                    (or (address %setAdmin)
                        (pair %setCurrency
                           (string %actionType)
                           (or %token
                              (address %fa12Token)
                              (pair %fa2Token (address %tokenContractAddress) (nat %tokenId))))))
                (or (or (pair %setLambda (string %name) (bytes %func_bytes)) (address %setSuperAdmin))
                    (or (pair %togglePauseEntrypoint
                           (or %targetEntrypoint
                              (or (or (bool %acceptOffer) (bool %createListing)) (or (bool %offer) (bool %purchase)))
                              (or (or (bool %removeListing) (bool %removeOffer)) (bool %setCurrency)))
                           (unit %empty))
                        (unit %unpauseAll)))))
        (or (or (pair %updateConfig
                   (nat %updateConfigNewValue)
                   (or %updateConfigAction (unit %configMinOfferAmount) (unit %configRoyalty)))
                (pair %updateGeneralContracts
                   (string %generalContractName)
                   (address %generalContractAddress)
                   (or %updateType (unit %remove) (unit %update))))
            (or (pair %updateMetadata (string %metadataKey) (bytes %metadataHash))
                (pair %updateWhitelistContracts
                   (string %whitelistContractName)
                   (address %whitelistContractAddress)
                   (or %updateType (unit %remove) (unit %update)))))) ;
  storage
    (pair (address %superAdmin)
          (option %newSuperAdmin address)
          (set %admins address)
          (big_map %metadata string bytes)
          (pair %config (nat %minOfferAmount) (nat %royalty))
          (pair %breakGlassConfig
             (bool %createListingIsPaused)
             (bool %removeListingIsPaused)
             (bool %purchaseIsPaused)
             (bool %offerIsPaused)
             (bool %acceptOfferIsPaused)
             (bool %removeOfferIsPaused)
             (bool %setCurrencyIsPaused))
          (map %whitelistContracts string address)
          (map %generalContracts string address)
          (nat %nextListingId)
          (nat %nextOfferId)
          (big_map %listingLedger
             nat
             (pair (address %initiator)
                   (or %token
                      (address %fa12Token)
                      (pair %fa2Token (address %tokenContractAddress) (nat %tokenId)))
                   (nat %price)
                   (nat %amount)
                   (or %currency
                      (or (address %fa12) (pair %fa2 (address %tokenContractAddress) (nat %tokenId)))
                      (unit %tez))
                   (option %expiryTime timestamp)))
          (big_map %offerLedger
             nat
             (pair (address %initiator)
                   (nat %listingId)
                   (nat %price)
                   (or %currency
                      (or (address %fa12) (pair %fa2 (address %tokenContractAddress) (nat %tokenId)))
                      (unit %tez))
                   (option %expiryTime timestamp)))
          (big_map %currencyLedger address (pair (string %tokenType) (set %tokenIds nat)))
          (map %lambdaLedger string bytes)) ;
  code { LAMBDA
           (pair string (map string bytes))
           bytes
           { UNPAIR ; GET ; IF_NONE { PUSH nat 2 ; FAILWITH } {} } ;
         LAMBDA
           (pair (pair bytes
                       (or (or (or (or (or nat unit)
                                       (or (pair address
                                                 (or address (pair address nat))
                                                 nat
                                                 nat
                                                 (option timestamp)
                                                 (or (or address (pair address nat)) unit))
                                           (list (pair address nat (or (or address (pair address nat)) unit)))))
                                   (or (or (pair nat nat (option timestamp) (or (or address (pair address nat)) unit)) unit)
                                       (or nat address)))
                               (or (or (or nat nat) (or address (pair string (or address (pair address nat)))))
                                   (or (or address (pair (or (or (or bool bool) (or bool bool)) (or (or bool bool) bool)) unit))
                                       (or unit (pair nat (or unit unit))))))
                           (or (or (pair string address (or unit unit)) (pair string bytes))
                               (pair string address (or unit unit)))))
                 address
                 (option address)
                 (set address)
                 (big_map string bytes)
                 (pair nat nat)
                 (pair bool bool bool bool bool bool bool)
                 (map string address)
                 (map string address)
                 nat
                 nat
                 (big_map
                    nat
                    (pair address
                          (or address (pair address nat))
                          nat
                          nat
                          (or (or address (pair address nat)) unit)
                          (option timestamp)))
                 (big_map
                    nat
                    (pair address nat nat (or (or address (pair address nat)) unit) (option timestamp)))
                 (big_map address (pair string (set nat)))
                 (map string bytes))
           (pair (list operation)
                 address
                 (option address)
                 (set address)
                 (big_map string bytes)
                 (pair nat nat)
                 (pair bool bool bool bool bool bool bool)
                 (map string address)
                 (map string address)
                 nat
                 nat
                 (big_map
                    nat
                    (pair address
                          (or address (pair address nat))
                          nat
                          nat
                          (or (or address (pair address nat)) unit)
                          (option timestamp)))
                 (big_map
                    nat
                    (pair address nat nat (or (or address (pair address nat)) unit) (option timestamp)))
                 (big_map address (pair string (set nat)))
                 (map string bytes))
           { UNPAIR ;
             UNPAIR ;
             UNPACK
               (lambda
                  (pair (or (or (or (or (or (nat %lambdaAcceptOffer) (unit %lambdaClaimSuperAdmin))
                                        (or (pair %lambdaCreateListing
                                               (address %initiator)
                                               (or %token
                                                  (address %fa12Token)
                                                  (pair %fa2Token (address %tokenContractAddress) (nat %tokenId)))
                                               (nat %price)
                                               (nat %amount)
                                               (option %expiryTime timestamp)
                                               (or %currency
                                                  (or (address %fa12) (pair %fa2 (address %tokenContractAddress) (nat %tokenId)))
                                                  (unit %tez)))
                                            (list %lambdaMistakenTransfer
                                               (pair (address %to_)
                                                     (nat %amount)
                                                     (or %token
                                                        (or (address %fa12) (pair %fa2 (address %tokenContractAddress) (nat %tokenId)))
                                                        (unit %tez))))))
                                    (or (or (pair %lambdaOffer
                                               (nat %listingId)
                                               (nat %price)
                                               (option %expiryTime timestamp)
                                               (or %currency
                                                  (or (address %fa12) (pair %fa2 (address %tokenContractAddress) (nat %tokenId)))
                                                  (unit %tez)))
                                            (unit %lambdaPauseAll))
                                        (or (nat %lambdaPurchase) (address %lambdaRemoveAdmin))))
                                (or (or (or (nat %lambdaRemoveListing) (nat %lambdaRemoveOffer))
                                        (or (address %lambdaSetAdmin)
                                            (pair %lambdaSetCurrency
                                               (string %actionType)
                                               (or %token
                                                  (address %fa12Token)
                                                  (pair %fa2Token (address %tokenContractAddress) (nat %tokenId))))))
                                    (or (or (address %lambdaSetSuperAdmin)
                                            (pair %lambdaTogglePauseEntrypoint
                                               (or %targetEntrypoint
                                                  (or (or (bool %acceptOffer) (bool %createListing)) (or (bool %offer) (bool %purchase)))
                                                  (or (or (bool %removeListing) (bool %removeOffer)) (bool %setCurrency)))
                                               (unit %empty)))
                                        (or (unit %lambdaUnpauseAll)
                                            (pair %lambdaUpdateConfig
                                               (nat %updateConfigNewValue)
                                               (or %updateConfigAction (unit %configMinOfferAmount) (unit %configRoyalty)))))))
                            (or (or (pair %lambdaUpdateGeneralContracts
                                       (string %generalContractName)
                                       (address %generalContractAddress)
                                       (or %updateType (unit %remove) (unit %update)))
                                    (pair %lambdaUpdateMetadata (string %metadataKey) (bytes %metadataHash)))
                                (pair %lambdaUpdateWhitelistContracts
                                   (string %whitelistContractName)
                                   (address %whitelistContractAddress)
                                   (or %updateType (unit %remove) (unit %update)))))
                        (address %superAdmin)
                        (option %newSuperAdmin address)
                        (set %admins address)
                        (big_map %metadata string bytes)
                        (pair %config (nat %minOfferAmount) (nat %royalty))
                        (pair %breakGlassConfig
                           (bool %createListingIsPaused)
                           (bool %removeListingIsPaused)
                           (bool %purchaseIsPaused)
                           (bool %offerIsPaused)
                           (bool %acceptOfferIsPaused)
                           (bool %removeOfferIsPaused)
                           (bool %setCurrencyIsPaused))
                        (map %whitelistContracts string address)
                        (map %generalContracts string address)
                        (nat %nextListingId)
                        (nat %nextOfferId)
                        (big_map %listingLedger
                           nat
                           (pair (address %initiator)
                                 (or %token
                                    (address %fa12Token)
                                    (pair %fa2Token (address %tokenContractAddress) (nat %tokenId)))
                                 (nat %price)
                                 (nat %amount)
                                 (or %currency
                                    (or (address %fa12) (pair %fa2 (address %tokenContractAddress) (nat %tokenId)))
                                    (unit %tez))
                                 (option %expiryTime timestamp)))
                        (big_map %offerLedger
                           nat
                           (pair (address %initiator)
                                 (nat %listingId)
                                 (nat %price)
                                 (or %currency
                                    (or (address %fa12) (pair %fa2 (address %tokenContractAddress) (nat %tokenId)))
                                    (unit %tez))
                                 (option %expiryTime timestamp)))
                        (big_map %currencyLedger address (pair (string %tokenType) (set %tokenIds nat)))
                        (map %lambdaLedger string bytes))
                  (pair (list operation)
                        (address %superAdmin)
                        (option %newSuperAdmin address)
                        (set %admins address)
                        (big_map %metadata string bytes)
                        (pair %config (nat %minOfferAmount) (nat %royalty))
                        (pair %breakGlassConfig
                           (bool %createListingIsPaused)
                           (bool %removeListingIsPaused)
                           (bool %purchaseIsPaused)
                           (bool %offerIsPaused)
                           (bool %acceptOfferIsPaused)
                           (bool %removeOfferIsPaused)
                           (bool %setCurrencyIsPaused))
                        (map %whitelistContracts string address)
                        (map %generalContracts string address)
                        (nat %nextListingId)
                        (nat %nextOfferId)
                        (big_map %listingLedger
                           nat
                           (pair (address %initiator)
                                 (or %token
                                    (address %fa12Token)
                                    (pair %fa2Token (address %tokenContractAddress) (nat %tokenId)))
                                 (nat %price)
                                 (nat %amount)
                                 (or %currency
                                    (or (address %fa12) (pair %fa2 (address %tokenContractAddress) (nat %tokenId)))
                                    (unit %tez))
                                 (option %expiryTime timestamp)))
                        (big_map %offerLedger
                           nat
                           (pair (address %initiator)
                                 (nat %listingId)
                                 (nat %price)
                                 (or %currency
                                    (or (address %fa12) (pair %fa2 (address %tokenContractAddress) (nat %tokenId)))
                                    (unit %tez))
                                 (option %expiryTime timestamp)))
                        (big_map %currencyLedger address (pair (string %tokenType) (set %tokenIds nat)))
                        (map %lambdaLedger string bytes))) ;
             IF_NONE { DROP 2 ; PUSH nat 3 ; FAILWITH } { DUG 2 ; PAIR ; EXEC } } ;
         DIG 2 ;
         UNPAIR ;
         IF_LEFT
           { IF_LEFT
               { IF_LEFT
                   { IF_LEFT
                       { IF_LEFT
                           { DUP 2 ;
                             GET 26 ;
                             PUSH string "lambdaAcceptOffer" ;
                             PAIR ;
                             DIG 4 ;
                             SWAP ;
                             EXEC ;
                             SWAP ;
                             LEFT unit ;
                             LEFT (or (pair address
                                            (or address (pair address nat))
                                            nat
                                            nat
                                            (option timestamp)
                                            (or (or address (pair address nat)) unit))
                                      (list (pair address nat (or (or address (pair address nat)) unit)))) ;
                             LEFT (or (or (pair nat nat (option timestamp) (or (or address (pair address nat)) unit)) unit)
                                      (or nat address)) ;
                             LEFT (or (or (or nat nat) (or address (pair string (or address (pair address nat)))))
                                      (or (or address (pair (or (or (or bool bool) (or bool bool)) (or (or bool bool) bool)) unit))
                                          (or unit (pair nat (or unit unit))))) ;
                             LEFT (or (or (pair string address (or unit unit)) (pair string bytes))
                                      (pair string address (or unit unit))) ;
                             DIG 2 ;
                             SWAP ;
                             DIG 2 ;
                             PAIR ;
                             PAIR ;
                             EXEC }
                           { DROP ;
                             DUP ;
                             GET 26 ;
                             PUSH string "lambdaClaimSuperAdmin" ;
                             PAIR ;
                             DIG 3 ;
                             SWAP ;
                             EXEC ;
                             UNIT ;
                             RIGHT nat ;
                             LEFT (or (pair address
                                            (or address (pair address nat))
                                            nat
                                            nat
                                            (option timestamp)
                                            (or (or address (pair address nat)) unit))
                                      (list (pair address nat (or (or address (pair address nat)) unit)))) ;
                             LEFT (or (or (pair nat nat (option timestamp) (or (or address (pair address nat)) unit)) unit)
                                      (or nat address)) ;
                             LEFT (or (or (or nat nat) (or address (pair string (or address (pair address nat)))))
                                      (or (or address (pair (or (or (or bool bool) (or bool bool)) (or (or bool bool) bool)) unit))
                                          (or unit (pair nat (or unit unit))))) ;
                             LEFT (or (or (pair string address (or unit unit)) (pair string bytes))
                                      (pair string address (or unit unit))) ;
                             DIG 2 ;
                             SWAP ;
                             DIG 2 ;
                             PAIR ;
                             PAIR ;
                             EXEC } }
                       { IF_LEFT
                           { DUP 2 ;
                             GET 26 ;
                             PUSH string "lambdaCreateListing" ;
                             PAIR ;
                             DIG 4 ;
                             SWAP ;
                             EXEC ;
                             SWAP ;
                             LEFT (list (pair address nat (or (or address (pair address nat)) unit))) ;
                             RIGHT (or nat unit) ;
                             LEFT (or (or (pair nat nat (option timestamp) (or (or address (pair address nat)) unit)) unit)
                                      (or nat address)) ;
                             LEFT (or (or (or nat nat) (or address (pair string (or address (pair address nat)))))
                                      (or (or address (pair (or (or (or bool bool) (or bool bool)) (or (or bool bool) bool)) unit))
                                          (or unit (pair nat (or unit unit))))) ;
                             LEFT (or (or (pair string address (or unit unit)) (pair string bytes))
                                      (pair string address (or unit unit))) ;
                             DIG 2 ;
                             SWAP ;
                             DIG 2 ;
                             PAIR ;
                             PAIR ;
                             EXEC }
                           { DUP 2 ;
                             GET 26 ;
                             PUSH string "lambdaMistakenTransfer" ;
                             PAIR ;
                             DIG 4 ;
                             SWAP ;
                             EXEC ;
                             SWAP ;
                             RIGHT
                               (pair address
                                     (or address (pair address nat))
                                     nat
                                     nat
                                     (option timestamp)
                                     (or (or address (pair address nat)) unit)) ;
                             RIGHT (or nat unit) ;
                             LEFT (or (or (pair nat nat (option timestamp) (or (or address (pair address nat)) unit)) unit)
                                      (or nat address)) ;
                             LEFT (or (or (or nat nat) (or address (pair string (or address (pair address nat)))))
                                      (or (or address (pair (or (or (or bool bool) (or bool bool)) (or (or bool bool) bool)) unit))
                                          (or unit (pair nat (or unit unit))))) ;
                             LEFT (or (or (pair string address (or unit unit)) (pair string bytes))
                                      (pair string address (or unit unit))) ;
                             DIG 2 ;
                             SWAP ;
                             DIG 2 ;
                             PAIR ;
                             PAIR ;
                             EXEC } } }
                   { IF_LEFT
                       { IF_LEFT
                           { DUP 2 ;
                             GET 26 ;
                             PUSH string "lambdaOffer" ;
                             PAIR ;
                             DIG 4 ;
                             SWAP ;
                             EXEC ;
                             SWAP ;
                             LEFT unit ;
                             LEFT (or nat address) ;
                             RIGHT
                               (or (or nat unit)
                                   (or (pair address
                                             (or address (pair address nat))
                                             nat
                                             nat
                                             (option timestamp)
                                             (or (or address (pair address nat)) unit))
                                       (list (pair address nat (or (or address (pair address nat)) unit))))) ;
                             LEFT (or (or (or nat nat) (or address (pair string (or address (pair address nat)))))
                                      (or (or address (pair (or (or (or bool bool) (or bool bool)) (or (or bool bool) bool)) unit))
                                          (or unit (pair nat (or unit unit))))) ;
                             LEFT (or (or (pair string address (or unit unit)) (pair string bytes))
                                      (pair string address (or unit unit))) ;
                             DIG 2 ;
                             SWAP ;
                             DIG 2 ;
                             PAIR ;
                             PAIR ;
                             EXEC }
                           { DROP ;
                             DUP ;
                             GET 26 ;
                             PUSH string "lambdaPauseAll" ;
                             PAIR ;
                             DIG 3 ;
                             SWAP ;
                             EXEC ;
                             UNIT ;
                             RIGHT (pair nat nat (option timestamp) (or (or address (pair address nat)) unit)) ;
                             LEFT (or nat address) ;
                             RIGHT
                               (or (or nat unit)
                                   (or (pair address
                                             (or address (pair address nat))
                                             nat
                                             nat
                                             (option timestamp)
                                             (or (or address (pair address nat)) unit))
                                       (list (pair address nat (or (or address (pair address nat)) unit))))) ;
                             LEFT (or (or (or nat nat) (or address (pair string (or address (pair address nat)))))
                                      (or (or address (pair (or (or (or bool bool) (or bool bool)) (or (or bool bool) bool)) unit))
                                          (or unit (pair nat (or unit unit))))) ;
                             LEFT (or (or (pair string address (or unit unit)) (pair string bytes))
                                      (pair string address (or unit unit))) ;
                             DIG 2 ;
                             SWAP ;
                             DIG 2 ;
                             PAIR ;
                             PAIR ;
                             EXEC } }
                       { IF_LEFT
                           { DUP 2 ;
                             GET 26 ;
                             PUSH string "lambdaPurchase" ;
                             PAIR ;
                             DIG 4 ;
                             SWAP ;
                             EXEC ;
                             SWAP ;
                             LEFT address ;
                             RIGHT (or (pair nat nat (option timestamp) (or (or address (pair address nat)) unit)) unit) ;
                             RIGHT
                               (or (or nat unit)
                                   (or (pair address
                                             (or address (pair address nat))
                                             nat
                                             nat
                                             (option timestamp)
                                             (or (or address (pair address nat)) unit))
                                       (list (pair address nat (or (or address (pair address nat)) unit))))) ;
                             LEFT (or (or (or nat nat) (or address (pair string (or address (pair address nat)))))
                                      (or (or address (pair (or (or (or bool bool) (or bool bool)) (or (or bool bool) bool)) unit))
                                          (or unit (pair nat (or unit unit))))) ;
                             LEFT (or (or (pair string address (or unit unit)) (pair string bytes))
                                      (pair string address (or unit unit))) ;
                             DIG 2 ;
                             SWAP ;
                             DIG 2 ;
                             PAIR ;
                             PAIR ;
                             EXEC }
                           { DUP 2 ;
                             GET 26 ;
                             PUSH string "lambdaRemoveAdmin" ;
                             PAIR ;
                             DIG 4 ;
                             SWAP ;
                             EXEC ;
                             SWAP ;
                             RIGHT nat ;
                             RIGHT (or (pair nat nat (option timestamp) (or (or address (pair address nat)) unit)) unit) ;
                             RIGHT
                               (or (or nat unit)
                                   (or (pair address
                                             (or address (pair address nat))
                                             nat
                                             nat
                                             (option timestamp)
                                             (or (or address (pair address nat)) unit))
                                       (list (pair address nat (or (or address (pair address nat)) unit))))) ;
                             LEFT (or (or (or nat nat) (or address (pair string (or address (pair address nat)))))
                                      (or (or address (pair (or (or (or bool bool) (or bool bool)) (or (or bool bool) bool)) unit))
                                          (or unit (pair nat (or unit unit))))) ;
                             LEFT (or (or (pair string address (or unit unit)) (pair string bytes))
                                      (pair string address (or unit unit))) ;
                             DIG 2 ;
                             SWAP ;
                             DIG 2 ;
                             PAIR ;
                             PAIR ;
                             EXEC } } } }
               { IF_LEFT
                   { IF_LEFT
                       { IF_LEFT
                           { DUP 2 ;
                             GET 26 ;
                             PUSH string "lambdaRemovelisting" ;
                             PAIR ;
                             DIG 4 ;
                             SWAP ;
                             EXEC ;
                             SWAP ;
                             LEFT nat ;
                             LEFT (or address (pair string (or address (pair address nat)))) ;
                             LEFT (or (or address (pair (or (or (or bool bool) (or bool bool)) (or (or bool bool) bool)) unit))
                                      (or unit (pair nat (or unit unit)))) ;
                             RIGHT
                               (or (or (or nat unit)
                                       (or (pair address
                                                 (or address (pair address nat))
                                                 nat
                                                 nat
                                                 (option timestamp)
                                                 (or (or address (pair address nat)) unit))
                                           (list (pair address nat (or (or address (pair address nat)) unit)))))
                                   (or (or (pair nat nat (option timestamp) (or (or address (pair address nat)) unit)) unit)
                                       (or nat address))) ;
                             LEFT (or (or (pair string address (or unit unit)) (pair string bytes))
                                      (pair string address (or unit unit))) ;
                             DIG 2 ;
                             SWAP ;
                             DIG 2 ;
                             PAIR ;
                             PAIR ;
                             EXEC }
                           { DUP 2 ;
                             GET 26 ;
                             PUSH string "lambdaRemoveOffer" ;
                             PAIR ;
                             DIG 4 ;
                             SWAP ;
                             EXEC ;
                             SWAP ;
                             RIGHT nat ;
                             LEFT (or address (pair string (or address (pair address nat)))) ;
                             LEFT (or (or address (pair (or (or (or bool bool) (or bool bool)) (or (or bool bool) bool)) unit))
                                      (or unit (pair nat (or unit unit)))) ;
                             RIGHT
                               (or (or (or nat unit)
                                       (or (pair address
                                                 (or address (pair address nat))
                                                 nat
                                                 nat
                                                 (option timestamp)
                                                 (or (or address (pair address nat)) unit))
                                           (list (pair address nat (or (or address (pair address nat)) unit)))))
                                   (or (or (pair nat nat (option timestamp) (or (or address (pair address nat)) unit)) unit)
                                       (or nat address))) ;
                             LEFT (or (or (pair string address (or unit unit)) (pair string bytes))
                                      (pair string address (or unit unit))) ;
                             DIG 2 ;
                             SWAP ;
                             DIG 2 ;
                             PAIR ;
                             PAIR ;
                             EXEC } }
                       { IF_LEFT
                           { DUP 2 ;
                             GET 26 ;
                             PUSH string "lambdaSetAdmin" ;
                             PAIR ;
                             DIG 4 ;
                             SWAP ;
                             EXEC ;
                             SWAP ;
                             LEFT (pair string (or address (pair address nat))) ;
                             RIGHT (or nat nat) ;
                             LEFT (or (or address (pair (or (or (or bool bool) (or bool bool)) (or (or bool bool) bool)) unit))
                                      (or unit (pair nat (or unit unit)))) ;
                             RIGHT
                               (or (or (or nat unit)
                                       (or (pair address
                                                 (or address (pair address nat))
                                                 nat
                                                 nat
                                                 (option timestamp)
                                                 (or (or address (pair address nat)) unit))
                                           (list (pair address nat (or (or address (pair address nat)) unit)))))
                                   (or (or (pair nat nat (option timestamp) (or (or address (pair address nat)) unit)) unit)
                                       (or nat address))) ;
                             LEFT (or (or (pair string address (or unit unit)) (pair string bytes))
                                      (pair string address (or unit unit))) ;
                             DIG 2 ;
                             SWAP ;
                             DIG 2 ;
                             PAIR ;
                             PAIR ;
                             EXEC }
                           { DUP 2 ;
                             GET 26 ;
                             PUSH string "lambdaSetCurrency" ;
                             PAIR ;
                             DIG 4 ;
                             SWAP ;
                             EXEC ;
                             SWAP ;
                             RIGHT address ;
                             RIGHT (or nat nat) ;
                             LEFT (or (or address (pair (or (or (or bool bool) (or bool bool)) (or (or bool bool) bool)) unit))
                                      (or unit (pair nat (or unit unit)))) ;
                             RIGHT
                               (or (or (or nat unit)
                                       (or (pair address
                                                 (or address (pair address nat))
                                                 nat
                                                 nat
                                                 (option timestamp)
                                                 (or (or address (pair address nat)) unit))
                                           (list (pair address nat (or (or address (pair address nat)) unit)))))
                                   (or (or (pair nat nat (option timestamp) (or (or address (pair address nat)) unit)) unit)
                                       (or nat address))) ;
                             LEFT (or (or (pair string address (or unit unit)) (pair string bytes))
                                      (pair string address (or unit unit))) ;
                             DIG 2 ;
                             SWAP ;
                             DIG 2 ;
                             PAIR ;
                             PAIR ;
                             EXEC } } }
                   { IF_LEFT
                       { IF_LEFT
                           { DIG 2 ;
                             DIG 3 ;
                             DROP 2 ;
                             SWAP ;
                             DUP ;
                             CAR ;
                             SENDER ;
                             SWAP ;
                             COMPARE ;
                             EQ ;
                             IF {} { PUSH nat 14 ; FAILWITH } ;
                             DUP ;
                             GET 26 ;
                             DUP 3 ;
                             CDR ;
                             DIG 3 ;
                             CAR ;
                             SWAP ;
                             SOME ;
                             SWAP ;
                             UPDATE ;
                             UPDATE 26 ;
                             NIL operation ;
                             PAIR }
                           { DUP 2 ;
                             GET 26 ;
                             PUSH string "lambdaSetSuperAdmin" ;
                             PAIR ;
                             DIG 4 ;
                             SWAP ;
                             EXEC ;
                             SWAP ;
                             LEFT (pair (or (or (or bool bool) (or bool bool)) (or (or bool bool) bool)) unit) ;
                             LEFT (or unit (pair nat (or unit unit))) ;
                             RIGHT (or (or nat nat) (or address (pair string (or address (pair address nat))))) ;
                             RIGHT
                               (or (or (or nat unit)
                                       (or (pair address
                                                 (or address (pair address nat))
                                                 nat
                                                 nat
                                                 (option timestamp)
                                                 (or (or address (pair address nat)) unit))
                                           (list (pair address nat (or (or address (pair address nat)) unit)))))
                                   (or (or (pair nat nat (option timestamp) (or (or address (pair address nat)) unit)) unit)
                                       (or nat address))) ;
                             LEFT (or (or (pair string address (or unit unit)) (pair string bytes))
                                      (pair string address (or unit unit))) ;
                             DIG 2 ;
                             SWAP ;
                             DIG 2 ;
                             PAIR ;
                             PAIR ;
                             EXEC } }
                       { IF_LEFT
                           { DUP 2 ;
                             GET 26 ;
                             PUSH string "lambdaTogglePauseEntrypoint" ;
                             PAIR ;
                             DIG 4 ;
                             SWAP ;
                             EXEC ;
                             SWAP ;
                             RIGHT address ;
                             LEFT (or unit (pair nat (or unit unit))) ;
                             RIGHT (or (or nat nat) (or address (pair string (or address (pair address nat))))) ;
                             RIGHT
                               (or (or (or nat unit)
                                       (or (pair address
                                                 (or address (pair address nat))
                                                 nat
                                                 nat
                                                 (option timestamp)
                                                 (or (or address (pair address nat)) unit))
                                           (list (pair address nat (or (or address (pair address nat)) unit)))))
                                   (or (or (pair nat nat (option timestamp) (or (or address (pair address nat)) unit)) unit)
                                       (or nat address))) ;
                             LEFT (or (or (pair string address (or unit unit)) (pair string bytes))
                                      (pair string address (or unit unit))) ;
                             DIG 2 ;
                             SWAP ;
                             DIG 2 ;
                             PAIR ;
                             PAIR ;
                             EXEC }
                           { DROP ;
                             DUP ;
                             GET 26 ;
                             PUSH string "lambdaUnpauseAll" ;
                             PAIR ;
                             DIG 3 ;
                             SWAP ;
                             EXEC ;
                             UNIT ;
                             LEFT (pair nat (or unit unit)) ;
                             RIGHT (or address (pair (or (or (or bool bool) (or bool bool)) (or (or bool bool) bool)) unit)) ;
                             RIGHT (or (or nat nat) (or address (pair string (or address (pair address nat))))) ;
                             RIGHT
                               (or (or (or nat unit)
                                       (or (pair address
                                                 (or address (pair address nat))
                                                 nat
                                                 nat
                                                 (option timestamp)
                                                 (or (or address (pair address nat)) unit))
                                           (list (pair address nat (or (or address (pair address nat)) unit)))))
                                   (or (or (pair nat nat (option timestamp) (or (or address (pair address nat)) unit)) unit)
                                       (or nat address))) ;
                             LEFT (or (or (pair string address (or unit unit)) (pair string bytes))
                                      (pair string address (or unit unit))) ;
                             DIG 2 ;
                             SWAP ;
                             DIG 2 ;
                             PAIR ;
                             PAIR ;
                             EXEC } } } } }
           { IF_LEFT
               { IF_LEFT
                   { DUP 2 ;
                     GET 26 ;
                     PUSH string "lambdaUpdateConfig" ;
                     PAIR ;
                     DIG 4 ;
                     SWAP ;
                     EXEC ;
                     SWAP ;
                     RIGHT unit ;
                     RIGHT (or address (pair (or (or (or bool bool) (or bool bool)) (or (or bool bool) bool)) unit)) ;
                     RIGHT (or (or nat nat) (or address (pair string (or address (pair address nat))))) ;
                     RIGHT
                       (or (or (or nat unit)
                               (or (pair address
                                         (or address (pair address nat))
                                         nat
                                         nat
                                         (option timestamp)
                                         (or (or address (pair address nat)) unit))
                                   (list (pair address nat (or (or address (pair address nat)) unit)))))
                           (or (or (pair nat nat (option timestamp) (or (or address (pair address nat)) unit)) unit)
                               (or nat address))) ;
                     LEFT (or (or (pair string address (or unit unit)) (pair string bytes))
                              (pair string address (or unit unit))) ;
                     DIG 2 ;
                     SWAP ;
                     DIG 2 ;
                     PAIR ;
                     PAIR ;
                     EXEC }
                   { DUP 2 ;
                     GET 26 ;
                     PUSH string "lambdaUpdateGeneralContracts" ;
                     PAIR ;
                     DIG 4 ;
                     SWAP ;
                     EXEC ;
                     SWAP ;
                     LEFT (pair string bytes) ;
                     LEFT (pair string address (or unit unit)) ;
                     RIGHT
                       (or (or (or (or nat unit)
                                   (or (pair address
                                             (or address (pair address nat))
                                             nat
                                             nat
                                             (option timestamp)
                                             (or (or address (pair address nat)) unit))
                                       (list (pair address nat (or (or address (pair address nat)) unit)))))
                               (or (or (pair nat nat (option timestamp) (or (or address (pair address nat)) unit)) unit)
                                   (or nat address)))
                           (or (or (or nat nat) (or address (pair string (or address (pair address nat)))))
                               (or (or address (pair (or (or (or bool bool) (or bool bool)) (or (or bool bool) bool)) unit))
                                   (or unit (pair nat (or unit unit)))))) ;
                     DIG 2 ;
                     SWAP ;
                     DIG 2 ;
                     PAIR ;
                     PAIR ;
                     EXEC } }
               { IF_LEFT
                   { DUP 2 ;
                     GET 26 ;
                     PUSH string "lambdaUpdateMetadata" ;
                     PAIR ;
                     DIG 4 ;
                     SWAP ;
                     EXEC ;
                     SWAP ;
                     RIGHT (pair string address (or unit unit)) ;
                     LEFT (pair string address (or unit unit)) ;
                     RIGHT
                       (or (or (or (or nat unit)
                                   (or (pair address
                                             (or address (pair address nat))
                                             nat
                                             nat
                                             (option timestamp)
                                             (or (or address (pair address nat)) unit))
                                       (list (pair address nat (or (or address (pair address nat)) unit)))))
                               (or (or (pair nat nat (option timestamp) (or (or address (pair address nat)) unit)) unit)
                                   (or nat address)))
                           (or (or (or nat nat) (or address (pair string (or address (pair address nat)))))
                               (or (or address (pair (or (or (or bool bool) (or bool bool)) (or (or bool bool) bool)) unit))
                                   (or unit (pair nat (or unit unit)))))) ;
                     DIG 2 ;
                     SWAP ;
                     DIG 2 ;
                     PAIR ;
                     PAIR ;
                     EXEC }
                   { DUP 2 ;
                     GET 26 ;
                     PUSH string "lambdaUpdateWhitelistContracts" ;
                     PAIR ;
                     DIG 4 ;
                     SWAP ;
                     EXEC ;
                     SWAP ;
                     RIGHT (or (pair string address (or unit unit)) (pair string bytes)) ;
                     RIGHT
                       (or (or (or (or nat unit)
                                   (or (pair address
                                             (or address (pair address nat))
                                             nat
                                             nat
                                             (option timestamp)
                                             (or (or address (pair address nat)) unit))
                                       (list (pair address nat (or (or address (pair address nat)) unit)))))
                               (or (or (pair nat nat (option timestamp) (or (or address (pair address nat)) unit)) unit)
                                   (or nat address)))
                           (or (or (or nat nat) (or address (pair string (or address (pair address nat)))))
                               (or (or address (pair (or (or (or bool bool) (or bool bool)) (or (or bool bool) bool)) unit))
                                   (or unit (pair nat (or unit unit)))))) ;
                     DIG 2 ;
                     SWAP ;
                     DIG 2 ;
                     PAIR ;
                     PAIR ;
                     EXEC } } } } }

