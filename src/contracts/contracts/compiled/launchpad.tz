{ parameter
    (or (or (or (or (or (unit %claimSuperAdmin) (nat %closeSale))
                    (or (pair %createTokenSale
                           (string %name)
                           (string %tokenIssuanceType)
                           (string %tokenDistributionType)
                           (address %tokenContractAddress)
                           (nat %tokenId)
                           (timestamp %saleStart)
                           (option %saleEnd timestamp)
                           (map %saleOptions
                              string
                              (pair (nat %maxAmountCap)
                                    (nat %totalBought)
                                    (option %maxAmountPerWalletTotal nat)
                                    (nat %price)
                                    (or %currency
                                       (or (address %fa12) (pair %fa2 (address %tokenContractAddress) (nat %tokenId)))
                                       (unit %tez))))
                           (map %defaultWhitelistOptions string nat))
                        (nat %distributeTokens)))
                (or (or (unit %editSale)
                        (list %mistakenTransfer
                           (pair (address %to_)
                                 (nat %amount)
                                 (or %token
                                    (or (address %fa12) (pair %fa2 (address %tokenContractAddress) (nat %tokenId)))
                                    (unit %tez)))))
                    (or (unit %pauseAll) (nat %pauseSale))))
            (or (or (or (pair %purchase (nat %saleId) (nat %amount) (string %saleOption))
                        (address %removeAdmin))
                    (or (address %setAdmin) (pair %setLambda (string %name) (bytes %func_bytes))))
                (or (or (list %setSaleWhitelist
                           (pair (nat %saleId)
                                 (address %whitelistUserAddress)
                                 (bool %defaultWhitelistOption)
                                 (option %whitelistOptions (map string nat))))
                        (address %setSuperAdmin))
                    (or (nat %startSale)
                        (pair %togglePauseEntrypoint
                           (or %targetEntrypoint
                              (or (or (or (bool %closeSale) (bool %createTokenSale))
                                      (or (bool %distributeTokens) (bool %editSale)))
                                  (or (or (bool %pauseSale) (bool %purchase))
                                      (or (bool %setSaleWhitelist) (bool %startSale))))
                              (bool %unpauseSale))
                           (unit %empty))))))
        (or (or (or (unit %unpauseAll) (nat %unpauseSale))
                (or (pair %updateConfig
                       (nat %updateConfigNewValue)
                       (or %updateConfigAction (unit %configMinOfferAmount) (unit %empty)))
                    (pair %updateGeneralContracts
                       (string %generalContractName)
                       (address %generalContractAddress)
                       (or %updateType (unit %remove) (unit %update)))))
            (or (pair %updateMetadata (string %metadataKey) (bytes %metadataHash))
                (pair %updateWhitelistContracts
                   (string %whitelistContractName)
                   (address %whitelistContractAddress)
                   (or %updateType (unit %remove) (unit %update)))))) ;
  storage
    (pair (address %superAdmin)
          (option %newSuperAdmin address)
          (set %admins address)
          (big_map %metadata string bytes)
          (pair %config (nat %minOfferAmount) (unit %empty))
          (pair %breakGlassConfig
             (bool %createTokenSaleIsPaused)
             (bool %startSaleIsPaused)
             (bool %closeSaleIsPaused)
             (bool %setSaleWhitelistIsPaused)
             (bool %editSaleIsPaused)
             (bool %pauseSaleIsPaused)
             (bool %unpauseSaleIsPaused)
             (bool %distributeTokensIsPaused)
             (bool %purchaseIsPaused))
          (map %whitelistContracts string address)
          (map %generalContracts string address)
          (big_map %saleLedger
             nat
             (pair (string %name)
                   (bool %isPaused)
                   (string %status)
                   (string %tokenIssuanceType)
                   (string %tokenDistributionType)
                   (address %tokenContractAddress)
                   (nat %tokenId)
                   (timestamp %saleStart)
                   (option %saleEnd timestamp)
                   (map %saleOptions
                      string
                      (pair (nat %maxAmountCap)
                            (nat %totalBought)
                            (option %maxAmountPerWalletTotal nat)
                            (nat %price)
                            (or %currency
                               (or (address %fa12) (pair %fa2 (address %tokenContractAddress) (nat %tokenId)))
                               (unit %tez))))
                   (map %defaultWhitelistOptions string nat)))
          (big_map %saleWhitelistLedger (pair nat address) (map string nat))
          (big_map %salePurchaseLedger
             (pair nat address)
             (pair (map %purchased string nat) (nat %totalPurchased)))
          (nat %lastSaleId)
          (map %lambdaLedger string bytes)) ;
  code { LAMBDA
           (pair string (map string bytes))
           bytes
           { UNPAIR ; GET ; IF_NONE { PUSH nat 2 ; FAILWITH } {} } ;
         LAMBDA
           (pair (pair bytes
                       (or (or (or (or (or unit nat)
                                       (or (pair string
                                                 string
                                                 string
                                                 address
                                                 nat
                                                 timestamp
                                                 (option timestamp)
                                                 (map string (pair nat nat (option nat) nat (or (or address (pair address nat)) unit)))
                                                 (map string nat))
                                           nat))
                                   (or (or unit (list (pair address nat (or (or address (pair address nat)) unit))))
                                       (or unit nat)))
                               (or (or (or (pair nat nat string) address)
                                       (or address (list (pair nat address bool (option (map string nat))))))
                                   (or (or address nat)
                                       (or (pair (or (or (or (or bool bool) (or bool bool)) (or (or bool bool) (or bool bool))) bool) unit)
                                           unit))))
                           (or (or (or nat (pair nat (or unit unit)))
                                   (or (pair string address (or unit unit)) (pair string bytes)))
                               (pair string address (or unit unit)))))
                 address
                 (option address)
                 (set address)
                 (big_map string bytes)
                 (pair nat unit)
                 (pair bool bool bool bool bool bool bool bool bool)
                 (map string address)
                 (map string address)
                 (big_map
                    nat
                    (pair string
                          bool
                          string
                          string
                          string
                          address
                          nat
                          timestamp
                          (option timestamp)
                          (map string (pair nat nat (option nat) nat (or (or address (pair address nat)) unit)))
                          (map string nat)))
                 (big_map (pair nat address) (map string nat))
                 (big_map (pair nat address) (pair (map string nat) nat))
                 nat
                 (map string bytes))
           (pair (list operation)
                 address
                 (option address)
                 (set address)
                 (big_map string bytes)
                 (pair nat unit)
                 (pair bool bool bool bool bool bool bool bool bool)
                 (map string address)
                 (map string address)
                 (big_map
                    nat
                    (pair string
                          bool
                          string
                          string
                          string
                          address
                          nat
                          timestamp
                          (option timestamp)
                          (map string (pair nat nat (option nat) nat (or (or address (pair address nat)) unit)))
                          (map string nat)))
                 (big_map (pair nat address) (map string nat))
                 (big_map (pair nat address) (pair (map string nat) nat))
                 nat
                 (map string bytes))
           { UNPAIR ;
             UNPAIR ;
             UNPACK
               (lambda
                  (pair (or (or (or (or (or (unit %lambdaClaimSuperAdmin) (nat %lambdaCloseSale))
                                        (or (pair %lambdaCreateTokenSale
                                               (string %name)
                                               (string %tokenIssuanceType)
                                               (string %tokenDistributionType)
                                               (address %tokenContractAddress)
                                               (nat %tokenId)
                                               (timestamp %saleStart)
                                               (option %saleEnd timestamp)
                                               (map %saleOptions
                                                  string
                                                  (pair (nat %maxAmountCap)
                                                        (nat %totalBought)
                                                        (option %maxAmountPerWalletTotal nat)
                                                        (nat %price)
                                                        (or %currency
                                                           (or (address %fa12) (pair %fa2 (address %tokenContractAddress) (nat %tokenId)))
                                                           (unit %tez))))
                                               (map %defaultWhitelistOptions string nat))
                                            (nat %lambdaDistributeTokens)))
                                    (or (or (unit %lambdaEditSale)
                                            (list %lambdaMistakenTransfer
                                               (pair (address %to_)
                                                     (nat %amount)
                                                     (or %token
                                                        (or (address %fa12) (pair %fa2 (address %tokenContractAddress) (nat %tokenId)))
                                                        (unit %tez)))))
                                        (or (unit %lambdaPauseAll) (nat %lambdaPauseSale))))
                                (or (or (or (pair %lambdaPurchase (nat %saleId) (nat %amount) (string %saleOption))
                                            (address %lambdaRemoveAdmin))
                                        (or (address %lambdaSetAdmin)
                                            (list %lambdaSetSaleWhitelist
                                               (pair (nat %saleId)
                                                     (address %whitelistUserAddress)
                                                     (bool %defaultWhitelistOption)
                                                     (option %whitelistOptions (map string nat))))))
                                    (or (or (address %lambdaSetSuperAdmin) (nat %lambdaStartSale))
                                        (or (pair %lambdaTogglePauseEntrypoint
                                               (or %targetEntrypoint
                                                  (or (or (or (bool %closeSale) (bool %createTokenSale))
                                                          (or (bool %distributeTokens) (bool %editSale)))
                                                      (or (or (bool %pauseSale) (bool %purchase))
                                                          (or (bool %setSaleWhitelist) (bool %startSale))))
                                                  (bool %unpauseSale))
                                               (unit %empty))
                                            (unit %lambdaUnpauseAll)))))
                            (or (or (or (nat %lambdaUnpauseSale)
                                        (pair %lambdaUpdateConfig
                                           (nat %updateConfigNewValue)
                                           (or %updateConfigAction (unit %configMinOfferAmount) (unit %empty))))
                                    (or (pair %lambdaUpdateGeneralContracts
                                           (string %generalContractName)
                                           (address %generalContractAddress)
                                           (or %updateType (unit %remove) (unit %update)))
                                        (pair %lambdaUpdateMetadata (string %metadataKey) (bytes %metadataHash))))
                                (pair %lambdaUpdateWhitelistContracts
                                   (string %whitelistContractName)
                                   (address %whitelistContractAddress)
                                   (or %updateType (unit %remove) (unit %update)))))
                        (address %superAdmin)
                        (option %newSuperAdmin address)
                        (set %admins address)
                        (big_map %metadata string bytes)
                        (pair %config (nat %minOfferAmount) (unit %empty))
                        (pair %breakGlassConfig
                           (bool %createTokenSaleIsPaused)
                           (bool %startSaleIsPaused)
                           (bool %closeSaleIsPaused)
                           (bool %setSaleWhitelistIsPaused)
                           (bool %editSaleIsPaused)
                           (bool %pauseSaleIsPaused)
                           (bool %unpauseSaleIsPaused)
                           (bool %distributeTokensIsPaused)
                           (bool %purchaseIsPaused))
                        (map %whitelistContracts string address)
                        (map %generalContracts string address)
                        (big_map %saleLedger
                           nat
                           (pair (string %name)
                                 (bool %isPaused)
                                 (string %status)
                                 (string %tokenIssuanceType)
                                 (string %tokenDistributionType)
                                 (address %tokenContractAddress)
                                 (nat %tokenId)
                                 (timestamp %saleStart)
                                 (option %saleEnd timestamp)
                                 (map %saleOptions
                                    string
                                    (pair (nat %maxAmountCap)
                                          (nat %totalBought)
                                          (option %maxAmountPerWalletTotal nat)
                                          (nat %price)
                                          (or %currency
                                             (or (address %fa12) (pair %fa2 (address %tokenContractAddress) (nat %tokenId)))
                                             (unit %tez))))
                                 (map %defaultWhitelistOptions string nat)))
                        (big_map %saleWhitelistLedger (pair nat address) (map string nat))
                        (big_map %salePurchaseLedger
                           (pair nat address)
                           (pair (map %purchased string nat) (nat %totalPurchased)))
                        (nat %lastSaleId)
                        (map %lambdaLedger string bytes))
                  (pair (list operation)
                        (address %superAdmin)
                        (option %newSuperAdmin address)
                        (set %admins address)
                        (big_map %metadata string bytes)
                        (pair %config (nat %minOfferAmount) (unit %empty))
                        (pair %breakGlassConfig
                           (bool %createTokenSaleIsPaused)
                           (bool %startSaleIsPaused)
                           (bool %closeSaleIsPaused)
                           (bool %setSaleWhitelistIsPaused)
                           (bool %editSaleIsPaused)
                           (bool %pauseSaleIsPaused)
                           (bool %unpauseSaleIsPaused)
                           (bool %distributeTokensIsPaused)
                           (bool %purchaseIsPaused))
                        (map %whitelistContracts string address)
                        (map %generalContracts string address)
                        (big_map %saleLedger
                           nat
                           (pair (string %name)
                                 (bool %isPaused)
                                 (string %status)
                                 (string %tokenIssuanceType)
                                 (string %tokenDistributionType)
                                 (address %tokenContractAddress)
                                 (nat %tokenId)
                                 (timestamp %saleStart)
                                 (option %saleEnd timestamp)
                                 (map %saleOptions
                                    string
                                    (pair (nat %maxAmountCap)
                                          (nat %totalBought)
                                          (option %maxAmountPerWalletTotal nat)
                                          (nat %price)
                                          (or %currency
                                             (or (address %fa12) (pair %fa2 (address %tokenContractAddress) (nat %tokenId)))
                                             (unit %tez))))
                                 (map %defaultWhitelistOptions string nat)))
                        (big_map %saleWhitelistLedger (pair nat address) (map string nat))
                        (big_map %salePurchaseLedger
                           (pair nat address)
                           (pair (map %purchased string nat) (nat %totalPurchased)))
                        (nat %lastSaleId)
                        (map %lambdaLedger string bytes))) ;
             IF_NONE { DROP 2 ; PUSH nat 3 ; FAILWITH } { DUG 2 ; PAIR ; EXEC } } ;
         DIG 2 ;
         UNPAIR ;
         IF_LEFT
           { IF_LEFT
               { IF_LEFT
                   { IF_LEFT
                       { IF_LEFT
                           { DROP ;
                             DUP ;
                             GET 24 ;
                             PUSH string "lambdaClaimSuperAdmin" ;
                             PAIR ;
                             DIG 3 ;
                             SWAP ;
                             EXEC ;
                             UNIT ;
                             LEFT nat ;
                             LEFT (or (pair string
                                            string
                                            string
                                            address
                                            nat
                                            timestamp
                                            (option timestamp)
                                            (map string (pair nat nat (option nat) nat (or (or address (pair address nat)) unit)))
                                            (map string nat))
                                      nat) ;
                             LEFT (or (or unit (list (pair address nat (or (or address (pair address nat)) unit))))
                                      (or unit nat)) ;
                             LEFT (or (or (or (pair nat nat string) address)
                                          (or address (list (pair nat address bool (option (map string nat))))))
                                      (or (or address nat)
                                          (or (pair (or (or (or (or bool bool) (or bool bool)) (or (or bool bool) (or bool bool))) bool) unit)
                                              unit))) ;
                             LEFT (or (or (or nat (pair nat (or unit unit)))
                                          (or (pair string address (or unit unit)) (pair string bytes)))
                                      (pair string address (or unit unit))) ;
                             DIG 2 ;
                             SWAP ;
                             DIG 2 ;
                             PAIR ;
                             PAIR ;
                             EXEC }
                           { DUP 2 ;
                             GET 24 ;
                             PUSH string "lambdaCloseSale" ;
                             PAIR ;
                             DIG 4 ;
                             SWAP ;
                             EXEC ;
                             SWAP ;
                             RIGHT unit ;
                             LEFT (or (pair string
                                            string
                                            string
                                            address
                                            nat
                                            timestamp
                                            (option timestamp)
                                            (map string (pair nat nat (option nat) nat (or (or address (pair address nat)) unit)))
                                            (map string nat))
                                      nat) ;
                             LEFT (or (or unit (list (pair address nat (or (or address (pair address nat)) unit))))
                                      (or unit nat)) ;
                             LEFT (or (or (or (pair nat nat string) address)
                                          (or address (list (pair nat address bool (option (map string nat))))))
                                      (or (or address nat)
                                          (or (pair (or (or (or (or bool bool) (or bool bool)) (or (or bool bool) (or bool bool))) bool) unit)
                                              unit))) ;
                             LEFT (or (or (or nat (pair nat (or unit unit)))
                                          (or (pair string address (or unit unit)) (pair string bytes)))
                                      (pair string address (or unit unit))) ;
                             DIG 2 ;
                             SWAP ;
                             DIG 2 ;
                             PAIR ;
                             PAIR ;
                             EXEC } }
                       { IF_LEFT
                           { DUP 2 ;
                             GET 24 ;
                             PUSH string "lambdaCreateTokenSale" ;
                             PAIR ;
                             DIG 4 ;
                             SWAP ;
                             EXEC ;
                             SWAP ;
                             LEFT nat ;
                             RIGHT (or unit nat) ;
                             LEFT (or (or unit (list (pair address nat (or (or address (pair address nat)) unit))))
                                      (or unit nat)) ;
                             LEFT (or (or (or (pair nat nat string) address)
                                          (or address (list (pair nat address bool (option (map string nat))))))
                                      (or (or address nat)
                                          (or (pair (or (or (or (or bool bool) (or bool bool)) (or (or bool bool) (or bool bool))) bool) unit)
                                              unit))) ;
                             LEFT (or (or (or nat (pair nat (or unit unit)))
                                          (or (pair string address (or unit unit)) (pair string bytes)))
                                      (pair string address (or unit unit))) ;
                             DIG 2 ;
                             SWAP ;
                             DIG 2 ;
                             PAIR ;
                             PAIR ;
                             EXEC }
                           { DUP 2 ;
                             GET 24 ;
                             PUSH string "lambdaDistributeTokens" ;
                             PAIR ;
                             DIG 4 ;
                             SWAP ;
                             EXEC ;
                             SWAP ;
                             RIGHT
                               (pair string
                                     string
                                     string
                                     address
                                     nat
                                     timestamp
                                     (option timestamp)
                                     (map string (pair nat nat (option nat) nat (or (or address (pair address nat)) unit)))
                                     (map string nat)) ;
                             RIGHT (or unit nat) ;
                             LEFT (or (or unit (list (pair address nat (or (or address (pair address nat)) unit))))
                                      (or unit nat)) ;
                             LEFT (or (or (or (pair nat nat string) address)
                                          (or address (list (pair nat address bool (option (map string nat))))))
                                      (or (or address nat)
                                          (or (pair (or (or (or (or bool bool) (or bool bool)) (or (or bool bool) (or bool bool))) bool) unit)
                                              unit))) ;
                             LEFT (or (or (or nat (pair nat (or unit unit)))
                                          (or (pair string address (or unit unit)) (pair string bytes)))
                                      (pair string address (or unit unit))) ;
                             DIG 2 ;
                             SWAP ;
                             DIG 2 ;
                             PAIR ;
                             PAIR ;
                             EXEC } } }
                   { IF_LEFT
                       { IF_LEFT
                           { DUP 2 ;
                             GET 24 ;
                             PUSH string "lambdaEditSale" ;
                             PAIR ;
                             DIG 4 ;
                             SWAP ;
                             EXEC ;
                             SWAP ;
                             LEFT (list (pair address nat (or (or address (pair address nat)) unit))) ;
                             LEFT (or unit nat) ;
                             RIGHT
                               (or (or unit nat)
                                   (or (pair string
                                             string
                                             string
                                             address
                                             nat
                                             timestamp
                                             (option timestamp)
                                             (map string (pair nat nat (option nat) nat (or (or address (pair address nat)) unit)))
                                             (map string nat))
                                       nat)) ;
                             LEFT (or (or (or (pair nat nat string) address)
                                          (or address (list (pair nat address bool (option (map string nat))))))
                                      (or (or address nat)
                                          (or (pair (or (or (or (or bool bool) (or bool bool)) (or (or bool bool) (or bool bool))) bool) unit)
                                              unit))) ;
                             LEFT (or (or (or nat (pair nat (or unit unit)))
                                          (or (pair string address (or unit unit)) (pair string bytes)))
                                      (pair string address (or unit unit))) ;
                             DIG 2 ;
                             SWAP ;
                             DIG 2 ;
                             PAIR ;
                             PAIR ;
                             EXEC }
                           { DUP 2 ;
                             GET 24 ;
                             PUSH string "lambdaMistakenTransfer" ;
                             PAIR ;
                             DIG 4 ;
                             SWAP ;
                             EXEC ;
                             SWAP ;
                             RIGHT unit ;
                             LEFT (or unit nat) ;
                             RIGHT
                               (or (or unit nat)
                                   (or (pair string
                                             string
                                             string
                                             address
                                             nat
                                             timestamp
                                             (option timestamp)
                                             (map string (pair nat nat (option nat) nat (or (or address (pair address nat)) unit)))
                                             (map string nat))
                                       nat)) ;
                             LEFT (or (or (or (pair nat nat string) address)
                                          (or address (list (pair nat address bool (option (map string nat))))))
                                      (or (or address nat)
                                          (or (pair (or (or (or (or bool bool) (or bool bool)) (or (or bool bool) (or bool bool))) bool) unit)
                                              unit))) ;
                             LEFT (or (or (or nat (pair nat (or unit unit)))
                                          (or (pair string address (or unit unit)) (pair string bytes)))
                                      (pair string address (or unit unit))) ;
                             DIG 2 ;
                             SWAP ;
                             DIG 2 ;
                             PAIR ;
                             PAIR ;
                             EXEC } }
                       { IF_LEFT
                           { DROP ;
                             DUP ;
                             GET 24 ;
                             PUSH string "lambdaPauseAll" ;
                             PAIR ;
                             DIG 3 ;
                             SWAP ;
                             EXEC ;
                             UNIT ;
                             LEFT nat ;
                             RIGHT (or unit (list (pair address nat (or (or address (pair address nat)) unit)))) ;
                             RIGHT
                               (or (or unit nat)
                                   (or (pair string
                                             string
                                             string
                                             address
                                             nat
                                             timestamp
                                             (option timestamp)
                                             (map string (pair nat nat (option nat) nat (or (or address (pair address nat)) unit)))
                                             (map string nat))
                                       nat)) ;
                             LEFT (or (or (or (pair nat nat string) address)
                                          (or address (list (pair nat address bool (option (map string nat))))))
                                      (or (or address nat)
                                          (or (pair (or (or (or (or bool bool) (or bool bool)) (or (or bool bool) (or bool bool))) bool) unit)
                                              unit))) ;
                             LEFT (or (or (or nat (pair nat (or unit unit)))
                                          (or (pair string address (or unit unit)) (pair string bytes)))
                                      (pair string address (or unit unit))) ;
                             DIG 2 ;
                             SWAP ;
                             DIG 2 ;
                             PAIR ;
                             PAIR ;
                             EXEC }
                           { DUP 2 ;
                             GET 24 ;
                             PUSH string "lambdaPauseSale" ;
                             PAIR ;
                             DIG 4 ;
                             SWAP ;
                             EXEC ;
                             SWAP ;
                             RIGHT unit ;
                             RIGHT (or unit (list (pair address nat (or (or address (pair address nat)) unit)))) ;
                             RIGHT
                               (or (or unit nat)
                                   (or (pair string
                                             string
                                             string
                                             address
                                             nat
                                             timestamp
                                             (option timestamp)
                                             (map string (pair nat nat (option nat) nat (or (or address (pair address nat)) unit)))
                                             (map string nat))
                                       nat)) ;
                             LEFT (or (or (or (pair nat nat string) address)
                                          (or address (list (pair nat address bool (option (map string nat))))))
                                      (or (or address nat)
                                          (or (pair (or (or (or (or bool bool) (or bool bool)) (or (or bool bool) (or bool bool))) bool) unit)
                                              unit))) ;
                             LEFT (or (or (or nat (pair nat (or unit unit)))
                                          (or (pair string address (or unit unit)) (pair string bytes)))
                                      (pair string address (or unit unit))) ;
                             DIG 2 ;
                             SWAP ;
                             DIG 2 ;
                             PAIR ;
                             PAIR ;
                             EXEC } } } }
               { IF_LEFT
                   { IF_LEFT
                       { IF_LEFT
                           { DUP 2 ;
                             GET 24 ;
                             PUSH string "lambdaPurchase" ;
                             PAIR ;
                             DIG 4 ;
                             SWAP ;
                             EXEC ;
                             SWAP ;
                             LEFT address ;
                             LEFT (or address (list (pair nat address bool (option (map string nat))))) ;
                             LEFT (or (or address nat)
                                      (or (pair (or (or (or (or bool bool) (or bool bool)) (or (or bool bool) (or bool bool))) bool) unit)
                                          unit)) ;
                             RIGHT
                               (or (or (or unit nat)
                                       (or (pair string
                                                 string
                                                 string
                                                 address
                                                 nat
                                                 timestamp
                                                 (option timestamp)
                                                 (map string (pair nat nat (option nat) nat (or (or address (pair address nat)) unit)))
                                                 (map string nat))
                                           nat))
                                   (or (or unit (list (pair address nat (or (or address (pair address nat)) unit))))
                                       (or unit nat))) ;
                             LEFT (or (or (or nat (pair nat (or unit unit)))
                                          (or (pair string address (or unit unit)) (pair string bytes)))
                                      (pair string address (or unit unit))) ;
                             DIG 2 ;
                             SWAP ;
                             DIG 2 ;
                             PAIR ;
                             PAIR ;
                             EXEC }
                           { DUP 2 ;
                             GET 24 ;
                             PUSH string "lambdaRemoveAdmin" ;
                             PAIR ;
                             DIG 4 ;
                             SWAP ;
                             EXEC ;
                             SWAP ;
                             RIGHT (pair nat nat string) ;
                             LEFT (or address (list (pair nat address bool (option (map string nat))))) ;
                             LEFT (or (or address nat)
                                      (or (pair (or (or (or (or bool bool) (or bool bool)) (or (or bool bool) (or bool bool))) bool) unit)
                                          unit)) ;
                             RIGHT
                               (or (or (or unit nat)
                                       (or (pair string
                                                 string
                                                 string
                                                 address
                                                 nat
                                                 timestamp
                                                 (option timestamp)
                                                 (map string (pair nat nat (option nat) nat (or (or address (pair address nat)) unit)))
                                                 (map string nat))
                                           nat))
                                   (or (or unit (list (pair address nat (or (or address (pair address nat)) unit))))
                                       (or unit nat))) ;
                             LEFT (or (or (or nat (pair nat (or unit unit)))
                                          (or (pair string address (or unit unit)) (pair string bytes)))
                                      (pair string address (or unit unit))) ;
                             DIG 2 ;
                             SWAP ;
                             DIG 2 ;
                             PAIR ;
                             PAIR ;
                             EXEC } }
                       { IF_LEFT
                           { DUP 2 ;
                             GET 24 ;
                             PUSH string "lambdaSetAdmin" ;
                             PAIR ;
                             DIG 4 ;
                             SWAP ;
                             EXEC ;
                             SWAP ;
                             LEFT (list (pair nat address bool (option (map string nat)))) ;
                             RIGHT (or (pair nat nat string) address) ;
                             LEFT (or (or address nat)
                                      (or (pair (or (or (or (or bool bool) (or bool bool)) (or (or bool bool) (or bool bool))) bool) unit)
                                          unit)) ;
                             RIGHT
                               (or (or (or unit nat)
                                       (or (pair string
                                                 string
                                                 string
                                                 address
                                                 nat
                                                 timestamp
                                                 (option timestamp)
                                                 (map string (pair nat nat (option nat) nat (or (or address (pair address nat)) unit)))
                                                 (map string nat))
                                           nat))
                                   (or (or unit (list (pair address nat (or (or address (pair address nat)) unit))))
                                       (or unit nat))) ;
                             LEFT (or (or (or nat (pair nat (or unit unit)))
                                          (or (pair string address (or unit unit)) (pair string bytes)))
                                      (pair string address (or unit unit))) ;
                             DIG 2 ;
                             SWAP ;
                             DIG 2 ;
                             PAIR ;
                             PAIR ;
                             EXEC }
                           { DIG 2 ;
                             DIG 3 ;
                             DROP 2 ;
                             SWAP ;
                             DUP ;
                             CAR ;
                             SENDER ;
                             SWAP ;
                             COMPARE ;
                             EQ ;
                             IF {} { PUSH nat 14 ; FAILWITH } ;
                             DUP ;
                             GET 24 ;
                             DUP 3 ;
                             CDR ;
                             DIG 3 ;
                             CAR ;
                             SWAP ;
                             SOME ;
                             SWAP ;
                             UPDATE ;
                             UPDATE 24 ;
                             NIL operation ;
                             PAIR } } }
                   { IF_LEFT
                       { IF_LEFT
                           { DUP 2 ;
                             GET 24 ;
                             PUSH string "lambdaSetSaleWhitelist" ;
                             PAIR ;
                             DIG 4 ;
                             SWAP ;
                             EXEC ;
                             SWAP ;
                             RIGHT address ;
                             RIGHT (or (pair nat nat string) address) ;
                             LEFT (or (or address nat)
                                      (or (pair (or (or (or (or bool bool) (or bool bool)) (or (or bool bool) (or bool bool))) bool) unit)
                                          unit)) ;
                             RIGHT
                               (or (or (or unit nat)
                                       (or (pair string
                                                 string
                                                 string
                                                 address
                                                 nat
                                                 timestamp
                                                 (option timestamp)
                                                 (map string (pair nat nat (option nat) nat (or (or address (pair address nat)) unit)))
                                                 (map string nat))
                                           nat))
                                   (or (or unit (list (pair address nat (or (or address (pair address nat)) unit))))
                                       (or unit nat))) ;
                             LEFT (or (or (or nat (pair nat (or unit unit)))
                                          (or (pair string address (or unit unit)) (pair string bytes)))
                                      (pair string address (or unit unit))) ;
                             DIG 2 ;
                             SWAP ;
                             DIG 2 ;
                             PAIR ;
                             PAIR ;
                             EXEC }
                           { DUP 2 ;
                             GET 24 ;
                             PUSH string "lambdaSetSuperAdmin" ;
                             PAIR ;
                             DIG 4 ;
                             SWAP ;
                             EXEC ;
                             SWAP ;
                             LEFT nat ;
                             LEFT (or (pair (or (or (or (or bool bool) (or bool bool)) (or (or bool bool) (or bool bool))) bool) unit)
                                      unit) ;
                             RIGHT
                               (or (or (pair nat nat string) address)
                                   (or address (list (pair nat address bool (option (map string nat)))))) ;
                             RIGHT
                               (or (or (or unit nat)
                                       (or (pair string
                                                 string
                                                 string
                                                 address
                                                 nat
                                                 timestamp
                                                 (option timestamp)
                                                 (map string (pair nat nat (option nat) nat (or (or address (pair address nat)) unit)))
                                                 (map string nat))
                                           nat))
                                   (or (or unit (list (pair address nat (or (or address (pair address nat)) unit))))
                                       (or unit nat))) ;
                             LEFT (or (or (or nat (pair nat (or unit unit)))
                                          (or (pair string address (or unit unit)) (pair string bytes)))
                                      (pair string address (or unit unit))) ;
                             DIG 2 ;
                             SWAP ;
                             DIG 2 ;
                             PAIR ;
                             PAIR ;
                             EXEC } }
                       { IF_LEFT
                           { DUP 2 ;
                             GET 24 ;
                             PUSH string "lambdaStartSale" ;
                             PAIR ;
                             DIG 4 ;
                             SWAP ;
                             EXEC ;
                             SWAP ;
                             RIGHT address ;
                             LEFT (or (pair (or (or (or (or bool bool) (or bool bool)) (or (or bool bool) (or bool bool))) bool) unit)
                                      unit) ;
                             RIGHT
                               (or (or (pair nat nat string) address)
                                   (or address (list (pair nat address bool (option (map string nat)))))) ;
                             RIGHT
                               (or (or (or unit nat)
                                       (or (pair string
                                                 string
                                                 string
                                                 address
                                                 nat
                                                 timestamp
                                                 (option timestamp)
                                                 (map string (pair nat nat (option nat) nat (or (or address (pair address nat)) unit)))
                                                 (map string nat))
                                           nat))
                                   (or (or unit (list (pair address nat (or (or address (pair address nat)) unit))))
                                       (or unit nat))) ;
                             LEFT (or (or (or nat (pair nat (or unit unit)))
                                          (or (pair string address (or unit unit)) (pair string bytes)))
                                      (pair string address (or unit unit))) ;
                             DIG 2 ;
                             SWAP ;
                             DIG 2 ;
                             PAIR ;
                             PAIR ;
                             EXEC }
                           { DUP 2 ;
                             GET 24 ;
                             PUSH string "lambdaTogglePauseEntrypoint" ;
                             PAIR ;
                             DIG 4 ;
                             SWAP ;
                             EXEC ;
                             SWAP ;
                             LEFT unit ;
                             RIGHT (or address nat) ;
                             RIGHT
                               (or (or (pair nat nat string) address)
                                   (or address (list (pair nat address bool (option (map string nat)))))) ;
                             RIGHT
                               (or (or (or unit nat)
                                       (or (pair string
                                                 string
                                                 string
                                                 address
                                                 nat
                                                 timestamp
                                                 (option timestamp)
                                                 (map string (pair nat nat (option nat) nat (or (or address (pair address nat)) unit)))
                                                 (map string nat))
                                           nat))
                                   (or (or unit (list (pair address nat (or (or address (pair address nat)) unit))))
                                       (or unit nat))) ;
                             LEFT (or (or (or nat (pair nat (or unit unit)))
                                          (or (pair string address (or unit unit)) (pair string bytes)))
                                      (pair string address (or unit unit))) ;
                             DIG 2 ;
                             SWAP ;
                             DIG 2 ;
                             PAIR ;
                             PAIR ;
                             EXEC } } } } }
           { IF_LEFT
               { IF_LEFT
                   { IF_LEFT
                       { DROP ;
                         DUP ;
                         GET 24 ;
                         PUSH string "lambdaUnpauseAll" ;
                         PAIR ;
                         DIG 3 ;
                         SWAP ;
                         EXEC ;
                         UNIT ;
                         RIGHT
                           (pair (or (or (or (or bool bool) (or bool bool)) (or (or bool bool) (or bool bool))) bool) unit) ;
                         RIGHT (or address nat) ;
                         RIGHT
                           (or (or (pair nat nat string) address)
                               (or address (list (pair nat address bool (option (map string nat)))))) ;
                         RIGHT
                           (or (or (or unit nat)
                                   (or (pair string
                                             string
                                             string
                                             address
                                             nat
                                             timestamp
                                             (option timestamp)
                                             (map string (pair nat nat (option nat) nat (or (or address (pair address nat)) unit)))
                                             (map string nat))
                                       nat))
                               (or (or unit (list (pair address nat (or (or address (pair address nat)) unit))))
                                   (or unit nat))) ;
                         LEFT (or (or (or nat (pair nat (or unit unit)))
                                      (or (pair string address (or unit unit)) (pair string bytes)))
                                  (pair string address (or unit unit))) ;
                         DIG 2 ;
                         SWAP ;
                         DIG 2 ;
                         PAIR ;
                         PAIR ;
                         EXEC }
                       { DUP 2 ;
                         GET 24 ;
                         PUSH string "lambdaUnpauseSale" ;
                         PAIR ;
                         DIG 4 ;
                         SWAP ;
                         EXEC ;
                         SWAP ;
                         LEFT (pair nat (or unit unit)) ;
                         LEFT (or (pair string address (or unit unit)) (pair string bytes)) ;
                         LEFT (pair string address (or unit unit)) ;
                         RIGHT
                           (or (or (or (or unit nat)
                                       (or (pair string
                                                 string
                                                 string
                                                 address
                                                 nat
                                                 timestamp
                                                 (option timestamp)
                                                 (map string (pair nat nat (option nat) nat (or (or address (pair address nat)) unit)))
                                                 (map string nat))
                                           nat))
                                   (or (or unit (list (pair address nat (or (or address (pair address nat)) unit))))
                                       (or unit nat)))
                               (or (or (or (pair nat nat string) address)
                                       (or address (list (pair nat address bool (option (map string nat))))))
                                   (or (or address nat)
                                       (or (pair (or (or (or (or bool bool) (or bool bool)) (or (or bool bool) (or bool bool))) bool) unit)
                                           unit)))) ;
                         DIG 2 ;
                         SWAP ;
                         DIG 2 ;
                         PAIR ;
                         PAIR ;
                         EXEC } }
                   { IF_LEFT
                       { DUP 2 ;
                         GET 24 ;
                         PUSH string "lambdaUpdateConfig" ;
                         PAIR ;
                         DIG 4 ;
                         SWAP ;
                         EXEC ;
                         SWAP ;
                         RIGHT nat ;
                         LEFT (or (pair string address (or unit unit)) (pair string bytes)) ;
                         LEFT (pair string address (or unit unit)) ;
                         RIGHT
                           (or (or (or (or unit nat)
                                       (or (pair string
                                                 string
                                                 string
                                                 address
                                                 nat
                                                 timestamp
                                                 (option timestamp)
                                                 (map string (pair nat nat (option nat) nat (or (or address (pair address nat)) unit)))
                                                 (map string nat))
                                           nat))
                                   (or (or unit (list (pair address nat (or (or address (pair address nat)) unit))))
                                       (or unit nat)))
                               (or (or (or (pair nat nat string) address)
                                       (or address (list (pair nat address bool (option (map string nat))))))
                                   (or (or address nat)
                                       (or (pair (or (or (or (or bool bool) (or bool bool)) (or (or bool bool) (or bool bool))) bool) unit)
                                           unit)))) ;
                         DIG 2 ;
                         SWAP ;
                         DIG 2 ;
                         PAIR ;
                         PAIR ;
                         EXEC }
                       { DUP 2 ;
                         GET 24 ;
                         PUSH string "lambdaUpdateGeneralContracts" ;
                         PAIR ;
                         DIG 4 ;
                         SWAP ;
                         EXEC ;
                         SWAP ;
                         LEFT (pair string bytes) ;
                         RIGHT (or nat (pair nat (or unit unit))) ;
                         LEFT (pair string address (or unit unit)) ;
                         RIGHT
                           (or (or (or (or unit nat)
                                       (or (pair string
                                                 string
                                                 string
                                                 address
                                                 nat
                                                 timestamp
                                                 (option timestamp)
                                                 (map string (pair nat nat (option nat) nat (or (or address (pair address nat)) unit)))
                                                 (map string nat))
                                           nat))
                                   (or (or unit (list (pair address nat (or (or address (pair address nat)) unit))))
                                       (or unit nat)))
                               (or (or (or (pair nat nat string) address)
                                       (or address (list (pair nat address bool (option (map string nat))))))
                                   (or (or address nat)
                                       (or (pair (or (or (or (or bool bool) (or bool bool)) (or (or bool bool) (or bool bool))) bool) unit)
                                           unit)))) ;
                         DIG 2 ;
                         SWAP ;
                         DIG 2 ;
                         PAIR ;
                         PAIR ;
                         EXEC } } }
               { IF_LEFT
                   { DUP 2 ;
                     GET 24 ;
                     PUSH string "lambdaUpdateMetadata" ;
                     PAIR ;
                     DIG 4 ;
                     SWAP ;
                     EXEC ;
                     SWAP ;
                     RIGHT (pair string address (or unit unit)) ;
                     RIGHT (or nat (pair nat (or unit unit))) ;
                     LEFT (pair string address (or unit unit)) ;
                     RIGHT
                       (or (or (or (or unit nat)
                                   (or (pair string
                                             string
                                             string
                                             address
                                             nat
                                             timestamp
                                             (option timestamp)
                                             (map string (pair nat nat (option nat) nat (or (or address (pair address nat)) unit)))
                                             (map string nat))
                                       nat))
                               (or (or unit (list (pair address nat (or (or address (pair address nat)) unit))))
                                   (or unit nat)))
                           (or (or (or (pair nat nat string) address)
                                   (or address (list (pair nat address bool (option (map string nat))))))
                               (or (or address nat)
                                   (or (pair (or (or (or (or bool bool) (or bool bool)) (or (or bool bool) (or bool bool))) bool) unit)
                                       unit)))) ;
                     DIG 2 ;
                     SWAP ;
                     DIG 2 ;
                     PAIR ;
                     PAIR ;
                     EXEC }
                   { DUP 2 ;
                     GET 24 ;
                     PUSH string "lambdaUpdateWhitelistContracts" ;
                     PAIR ;
                     DIG 4 ;
                     SWAP ;
                     EXEC ;
                     SWAP ;
                     RIGHT
                       (or (or nat (pair nat (or unit unit)))
                           (or (pair string address (or unit unit)) (pair string bytes))) ;
                     RIGHT
                       (or (or (or (or unit nat)
                                   (or (pair string
                                             string
                                             string
                                             address
                                             nat
                                             timestamp
                                             (option timestamp)
                                             (map string (pair nat nat (option nat) nat (or (or address (pair address nat)) unit)))
                                             (map string nat))
                                       nat))
                               (or (or unit (list (pair address nat (or (or address (pair address nat)) unit))))
                                   (or unit nat)))
                           (or (or (or (pair nat nat string) address)
                                   (or address (list (pair nat address bool (option (map string nat))))))
                               (or (or address nat)
                                   (or (pair (or (or (or (or bool bool) (or bool bool)) (or (or bool bool) (or bool bool))) bool) unit)
                                       unit)))) ;
                     DIG 2 ;
                     SWAP ;
                     DIG 2 ;
                     PAIR ;
                     PAIR ;
                     EXEC } } } } }

